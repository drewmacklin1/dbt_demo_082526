name: 'acme_field_force'
version: '1.0.0'
profile: 'default'          # <-- Partner Connect usually names this 'default'. Confirm in your environment.

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

# =====================================================================
# CONFIRM THESE MATCH THE DATABASES AND SCHEMAS YOU CREATED.
# Sources live in DBT_DEMO_BRZ.SOURCE_A (set in models/BRZ/_sources_*.yml).
# =====================================================================
models:
  acme_field_force:

    SLV:
      +materialized: view
      +database: DBT_DEMO_SLV
      FCT:
        +schema: FCT
      DIM:
        +schema: DIM

    GLD:
      +materialized: table
      +database: DBT_DEMO_GLD
      +schema: OUTPUT
flags:
  require_generic_test_arguments_property: true